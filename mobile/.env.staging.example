# Staging secrets — NEVER commit a filled copy. NEVER reuse production values.

# --- Client (Vite) ---
VITE_API_URL=https://api.staging.example.com
VITE_SOCKET_URL=https://api.staging.example.com
VITE_APP_URL=https://staging.example.com
VITE_USE_LOCAL_DEMO=false
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# --- Legacy Next public names (if any tooling still reads them) ---
NEXT_PUBLIC_BACKEND_PROVIDER=api
NEXT_PUBLIC_API_URL=https://api.staging.example.com
NEXT_PUBLIC_SOCKET_URL=https://api.staging.example.com
NEXT_PUBLIC_APP_URL=https://staging.example.com

# --- Compose / data plane (isolated staging DB only) ---
POSTGRES_USER=hel
POSTGRES_PASSWORD=change_me_staging_pg
POSTGRES_DB=hel_staging
MINIO_ROOT_USER=helminio_staging
MINIO_ROOT_PASSWORD=change_me_staging_minio
SESSION_SECRET=change_me_to_a_long_random_staging_secret_32
COOKIE_DOMAIN=.staging.example.com
CORS_ORIGINS=https://staging.example.com
APP_URL=https://staging.example.com
# Staging may use fake Stripe / console mail; production forbids both
STRIPE_GATEWAY=fake
STRIPE_ALLOW_LIVE=false
MAIL_DRIVER=console
