# Paste into Vercel → Project → Settings → Environment Variables
# Custom domain: https://www.helcalafkaaga.com (also keep the Vercel URL if used)
#
# IMPORTANT: These NEXT_PUBLIC_* vars must be set BEFORE / during the Vercel
# build (not only runtime). After changing them, trigger a Redeploy.
#
# Do not point API_URL / SOCKET_URL at the Vercel/frontend URL.

NEXT_PUBLIC_APP_URL=https://www.helcalafkaaga.com
NEXT_PUBLIC_API_URL=https://tel-calafkaaga-1.onrender.com
NEXT_PUBLIC_SOCKET_URL=https://tel-calafkaaga-1.onrender.com

# ---------------------------------------------------------------------------
# Render Nest service env (Dashboard → tel-calafkaaga-1 → Environment)
# Paste these — then Manual Deploy / restart. Fixes browser CORS errors.
# Include EVERY frontend origin that will call the API (comma-separated, no spaces).
# ---------------------------------------------------------------------------
CORS_ORIGINS=https://www.helcalafkaaga.com,https://helcalafkaaga.com,https://tel-calafkaaga-1-api-one.vercel.app
APP_URL=https://www.helcalafkaaga.com
COOKIE_SECURE=true
COOKIE_SAMESITE=none
TRUST_PROXY=true
# DATABASE_URL=<Render Postgres URL>
# REDIS_URL=...
# SESSION_SECRET=<min 32 chars>
# S3_* = Cloudflare R2 or other public cloud bucket (not localhost MinIO)
#
# R2 CORS — run: set -a && source TEL-CALAFKAAGA-1.env && set +a && ./infra/staging/apply-r2-cors.sh
# (script allows www + apex + Vercel origins for GET/HEAD/PUT)
# S3_BUCKET_PROFILE=hel-profile
# S3_BUCKET_PROFILE_PRIVATE=hel-profile-private
# S3_BUCKET_CHAT=hel-chat
# S3_BUCKET_SUPPORT=hel-support
# S3_BUCKET_EVC=hel-evc
# S3_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# S3_REGION=auto
# S3_FORCE_PATH_STYLE=false
