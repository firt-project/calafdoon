# Public client env for production / physical-phone builds against Render.
# Copy to apps/client/.env.production (gitignored) or pass the same vars at build time.
# Never put DATABASE_URL, JWT, Redis, Stripe secrets, or S3 keys here.

VITE_API_URL=https://tel-calafkaaga-1.onrender.com
VITE_SOCKET_URL=https://tel-calafkaaga-1.onrender.com
VITE_APP_URL=https://helcalafkaaga.com
VITE_USE_LOCAL_DEMO=false
